# Copy this file to .env.local (Next.js local env) and update values.
# PowerShell example to set env for local dev (see README)

# Public site URL used for sitemap and JSON-LD
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Google Analytics (GA4) Measurement ID (optional)
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Google AdSense client (optional) - example ca-pub-XXXXXXXX
NEXT_PUBLIC_ADSENSE_CLIENT=ca-pub-XXXXXXXXXXXX

# Optional AdSense slot id for placements
NEXT_PUBLIC_ADSENSE_SLOT=1234567890

# Admin credentials (used by the simplistic admin login API)
ADMIN_USER=Superadmin
ADMIN_PASS=Adminuser@123

# Database URL (MySQL) - example format
# mysql://user:pass@host:port/database?ssl-mode=REQUIRED
## DATABASE_URL is sensitive. Set this in your deployment environment or in a local `.env.local` file.
## Example format (do NOT commit real credentials):
## mysql://user:pass@host:port/database?ssl-mode=REQUIRED
DATABASE_URL=

# JWT secret used to sign admin tokens
ADMIN_JWT_SECRET=

# Optional external conversion service (use a provider that supports PDF -> Office conversions)
# If set, the server will POST uploads to this URL and return the converted file.
# Example using ConvertAPI (free tier available):
# EXTERNAL_CONVERSION_API_URL=https://v2.convertapi.com/convert/pdf/to/docx
# EXTERNAL_CONVERSION_API_KEY=your_convertapi_secret_here
EXTERNAL_CONVERSION_API_URL=
# Optional API key for the external conversion service
EXTERNAL_CONVERSION_API_KEY=
